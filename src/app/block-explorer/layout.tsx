const Layout = ({
    children,
}: {
    children: React.ReactNode
}) => {
    // Chain List sidebar left
    // Search header
    // Table header
    return (
        <div>
            {children}
        </div>
    )
}

export default Layout;